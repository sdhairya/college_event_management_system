import 'package:flutter/material.dart';

import 'package:college_event_management/editProfile/components/body.dart';

class editProfile extends StatelessWidget {
  // String? fname, lname, email, eno, mobile, branch, sem, college, address;
  const editProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return body();
  }
}
