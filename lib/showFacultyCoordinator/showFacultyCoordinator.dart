import 'package:flutter/material.dart';
import '../showFacultyCoordinator/components/body.dart';

class showFacultyCoordinator extends StatelessWidget {
  const showFacultyCoordinator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return body();
  }
}
