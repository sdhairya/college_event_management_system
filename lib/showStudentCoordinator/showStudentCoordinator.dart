import 'package:flutter/material.dart';

import '../showStudentCoordinator/components/body.dart';

class showStudentCoordinator extends StatelessWidget {
  const showStudentCoordinator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return body();
  }
}
